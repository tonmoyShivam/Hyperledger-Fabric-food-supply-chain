{
  "name": "food-traceability",
  "version": "1.0.0",
  "description": "Hyperledger Fabric chaincode for food supply chain traceability",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "fabric-chaincode-node start",
    "test": "mocha --recursive --timeout 10000 --exit test"
  },
  "dependencies": {
    "fabric-contract-api": "^2.5.8",
    "fabric-shim": "^2.5.8"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^10.8.2",
    "sinon": "^19.0.2",
    "sinon-chai": "^3.7.0"
  }
}
